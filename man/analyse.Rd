% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{analyse}
\alias{analyse}
\alias{llm_analyse}
\alias{llm_code}
\alias{llm_explain}
\alias{llm_suggest_plot}
\title{Data Analysis and Code Assistance}
\usage{
llm_analyse(data, question = NULL, new = FALSE, ...)

llm_code(data = NULL, task, style = c("tidyverse", "base"), new = FALSE, ...)

llm_explain(code, detail = c("brief", "detailed"), new = FALSE, ...)

llm_suggest_plot(data, goal = NULL, new = FALSE, ...)
}
\arguments{
\item{data}{A data frame. Summarised before sending to the LLM; raw rows are
never sent verbatim.}

\item{question}{A specific question about the data. If \code{NULL}, the LLM is
asked to suggest appropriate analyses.}

\item{new}{Logical. If \code{TRUE}, re-initiates the LLM before the task call.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[=initiate_llm]{initiate_llm()}}.}

\item{task}{Description of what the R code should accomplish.}

\item{style}{Coding style for generated R code: \code{"tidyverse"} (default) or
\code{"base"}.}

\item{code}{A character string containing R code to explain or improve.}

\item{detail}{Level of detail for explanations: \code{"brief"} (one paragraph,
default) or \code{"detailed"} (step-by-step).}

\item{goal}{Optional description of what the plot should show.}
}
\value{
A character string with the LLM response (suggestions, code, or
explanation).
}
\description{
These functions use the active LLM to assist with data analysis and R code
tasks. Each call uses an isolated chat context. Generated code is returned
as a character string for the user to review and run manually.
\itemize{
\item \code{llm_analyse()} suggests analyses or answers a question about a dataset.
\item \code{llm_code()} generates R code for a described task.
\item \code{llm_explain()} explains R code in Dutch.
\item \code{llm_suggest_plot()} suggests a \code{plot2()} call for a dataset.
}
}

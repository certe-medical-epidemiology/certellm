% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat_history.R
\name{chat_history}
\alias{chat_history}
\alias{save_chat}
\alias{restore_chat}
\alias{list_chats}
\alias{reset_chat}
\title{Save and Restore Chat Conversations}
\usage{
save_chat(name = format(Sys.time(), "\%Y\%m\%d_\%H\%M\%S"))

restore_chat(name)

list_chats()

reset_chat()
}
\arguments{
\item{name}{Name under which to save or from which to restore the snapshot.
Defaults to a timestamp (\code{"YYYYMMDD_HHMMSS"}).}
}
\value{
\code{save_chat()} and \code{reset_chat()} return \code{invisible(NULL)}.
\code{restore_chat()} returns \code{invisible(NULL)}. \code{list_chats()} returns a
character vector of snapshot names (or an empty vector if none exist).
}
\description{
These functions manage the turn history of the active LLM conversation so
that sessions can be saved, restored, listed, or reset.
\itemize{
\item \code{save_chat()} snapshots the current conversation under a name.
\item \code{restore_chat()} replaces the current conversation with a saved snapshot.
\item \code{list_chats()} returns the names of all saved snapshots.
\item \code{reset_chat()} clears the current conversation turn history.
}
}

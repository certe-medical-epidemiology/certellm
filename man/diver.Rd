% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diver.R
\name{diver}
\alias{diver}
\alias{llm_diver_query}
\alias{llm_diver_explain}
\title{Diver Database Query Assistance}
\usage{
llm_diver_query(
  description,
  table = NULL,
  columns_hint = NULL,
  new = FALSE,
  ...
)

llm_diver_explain(query, new = FALSE, ...)
}
\arguments{
\item{description}{Natural language description of the data to retrieve,
e.g. \code{"All E. coli urine cultures from 2024 in Groningen"}.}

\item{table}{Optional hint for the Diver table to query.}

\item{columns_hint}{Optional character vector of known column names to
assist the LLM in forming the query.}

\item{new}{Logical. If \code{TRUE}, re-initiates the LLM before the task call.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[=initiate_llm]{initiate_llm()}}.}

\item{query}{An existing \code{get_diver_data()} call as a character string,
to be explained.}
}
\value{
A character string: the generated R code (\code{llm_diver_query()}) or
the Dutch explanation (\code{llm_diver_explain()}).
}
\description{
These functions help construct and explain \code{get_diver_data()} calls (from
the \code{certedb} package) using the active LLM. Each call uses an isolated
chat context so the main conversation history is not affected.
\itemize{
\item \code{llm_diver_query()} generates a \code{get_diver_data()} call from a natural
language description.
\item \code{llm_diver_explain()} explains an existing \code{get_diver_data()} call in
plain Dutch.
}

The LLM is primed with a static schema hint describing known Diver column
names and conventions. When the exact column name is uncertain, the generated
code includes a comment noting the assumption.
}

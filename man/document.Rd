% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document.R
\name{document}
\alias{document}
\alias{llm_review_document}
\alias{llm_write_section}
\alias{llm_write_chunk}
\title{Rmd / Quarto Document Assistance}
\usage{
llm_review_document(
  file = NULL,
  text = NULL,
  focus = c("all", "language", "structure", "completeness"),
  new = FALSE,
  ...
)

llm_write_section(
  section,
  data = NULL,
  context = NULL,
  style = c("report", "presentation", "article"),
  new = FALSE,
  ...
)

llm_write_chunk(code = NULL, goal = NULL, new = FALSE, ...)
}
\arguments{
\item{file}{Path to an \code{.Rmd} or \code{.qmd} file. The first 3000 lines are read.
Either \code{file} or \code{text} must be supplied.}

\item{text}{The document content as a character string. Either \code{file} or
\code{text} must be supplied.}

\item{focus}{What to focus on during review: \code{"language"}, \code{"structure"},
\code{"completeness"}, or \code{"all"} (default).}

\item{new}{Logical. If \code{TRUE}, re-initiates the LLM before the task call.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to the LLM chat method.}

\item{section}{Name of the section to write, e.g. \code{"Methods"}, \code{"Results"},
\code{"Discussion"}.}

\item{data}{Optional data frame providing context for the section. Summarised
before sending to the LLM.}

\item{context}{Optional character string with additional background context
for the section (e.g. a description of the analysis).}

\item{style}{Document style: \code{"report"} (default), \code{"presentation"}, or
\code{"article"}.}

\item{code}{Optional existing R code to improve (as a character string).}

\item{goal}{Description of what the code chunk should accomplish. Required
when \code{code} is \code{NULL}.}
}
\value{
A character string with the generated or reviewed text.
}
\description{
These functions use the active LLM to assist with writing and reviewing
R Markdown and Quarto documents. Each call uses an isolated chat context.
\itemize{
\item \code{llm_review_document()} reviews a document for language, structure, or
completeness and returns Dutch-language feedback.
\item \code{llm_write_section()} writes a Dutch section (e.g. Methods, Results) for
a report, presentation, or article.
\item \code{llm_write_chunk()} generates or improves an R code chunk for a document.
}
}
